all:
  children:
    # --- Instance Family ---
    berry:
      hosts:
        berry[1:3]:
    # --- OS ---
    ubuntu:
      hosts:
        berry[1:3]:
        # desk1:
    nixos:
      hosts:
        core1:
        oracle[1:3]:
